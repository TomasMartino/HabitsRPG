# =============================================================
# PROD Profile — PostgreSQL (Supabase) for production
# =============================================================
# IMPORTANT: Set these via environment variables, NOT hardcoded
# SPRING_DATASOURCE_URL, SPRING_DATASOURCE_USERNAME, SPRING_DATASOURCE_PASSWORD

spring.datasource.url=jdbc:postgresql://aws-0-us-west-2.pooler.supabase.com:5432/postgres?user=postgres.cpoojwxhqvooyksidbeu&password=${SUPABASE_DB_PASSWORD}
spring.datasource.username=postgres.cpoojwxhqvooyksidbeu
spring.datasource.password=${SUPABASE_DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# In production, validate schema — never auto-update
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
